#!/bin/bash
BRANCH=`git branch | grep \* | cut -d ' ' -f2`
BRANCHX=`git branch`
echo Found branch $BRANCH
echo Found branch $BRANCHX

if [ $BRANCH == 'master' ]
  then
    echo Publishing to Sonatype
    sbt publish sonatypeReleaseAll
fi
